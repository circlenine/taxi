# このリポジトリでの約束

大阪のタクシー乗務員グループで使う、記録用スプレッドシートの
Google Apps Script です。作業はほぼ iPhone だけで行われます。

## エラーを出したときの決まり（いちばん大事）

**まず謝る。原因の説明はそのあと。**

1. 返事のいちばん最初に「**申し訳ありませんでした**」と書く。
   「すみません」で済ませない。説明を先に立てて、謝罪を後ろに
   小さく付けるのもしない。
2. 何を、どう間違えたのかを、はっきり書く。言いわけをしない。
3. **防げたものだったかどうか**を正直に書く。防げたなら、そう認める。
4. 同じことが二度と起きないよう、**テストを足す**。
   テストが見逃していたなら、テストのほうも直す。
   直したら、わざとバグを戻して「ちゃんと落ちる」ことを確かめる。
5. 報告のときは、うまくいったことだけでなく、**できなかったこと・
   確かめられなかったこと**も必ず書く。

## 返事・知らせの書き方の決まり

- **行の頭に「・」を付けて並べる。** べた書きの長い文は、
  スマホでは折り返しだらけになって、どこが1つのまとまりか分からない。
- **短くする。** LINEの知らせは5行まで。長い英語のエラーはそのまま貼らない。
  「何が起きたか」と「どうすればよいか」だけを書く。
- **毎回おなじ説明を出さない。** 1回分かれば済む注釈は、マニュアルへ。
- **届かない直し方を案内しない。** LINEが効かない話のときに
  「LINEで〇〇と送ってください」と書かない。スプシのボタンへ案内する。

## 見た目・呼び出しの決まり

- **記録用スプシのタブ名は、絵文字なし・全角2文字以内**にする。
  （例：「天気」）。タブが多く、長い名前や絵文字が混ざると、
  スマホではタブの行がすぐ埋まって、目当てのものを探せない。
- **LINEからの呼び出しは、絵文字ひとつで済む形にする。**
  走りながら打つので、字を打たせない。言葉での呼び方も残してよいが、
  新しい呼び出しを作るときは、必ず絵文字ひとつを割り当てる。

## 手順を書くときの決まり

**指を動かす手順は、はじめての人がそのとおりにやれば終わるところまで書く。**
毎回言わせない。

1. **番号を振る**（①②③）。1つの番号でやることは1つだけ。
2. **押すものは、画面に出ている名前をそのまま書く。**
   「設定から」ではなく「右上の【デプロイ】→【デプロイを管理】」。
3. **iPhoneでやる前提**で書く。パソコンのほうが早いときは、そう書く。
4. **そこで何が見えるはずか**を書く。見えなければ、どこで間違えたかが分かる。
5. **つまずきやすいところ**を、先に書いておく。
6. **手順は `docs/` の .md ではなく、マニュアルのスプシに入れる。**
   スマホでは .md は字が小さく・横に流れ・表が崩れて、読めない。
   `gas/008-Manual.gs` の中身を直して、[16] で作り直す。
   タブ名はここでも**絵文字なし・全角2文字**。
7. **1行は全角42文字まで。** それより長いと、細い画面で折り返しだらけになる。

## そのほかの決まり

- 直したら、ファイル先頭のバージョンを1つ上げ、履歴に何を直したか書く。
  記号はファイル名の頭文字（C/E/L/W/U/V）。詳しくは `gas/README.md`。
- **指示されていないものを、勝手に消さない。**
- 「他にも考えうるエラー対策」も、あわせて必ず入れる。
- コメントは、プログラムを知らない人が読んで分かる日本語で書く。
  「なぜそうしたか」を書く。「何をしているか」だけでは足りない。
- 鍵（LINEトークン・Geminiキー・GitHubトークン・DiscordのURL）は
  スクリプトプロパティにだけ置く。コードにもシートにもリポジトリにも書かない。
- グループLINEへの本番送信は、必ず2段階で確かめる。
- メールアドレスをリポジトリに書かない。

## 作業の流れ

```
cd gas/test && for f in *.test.js; do node "$f" >/dev/null || echo "FAIL $f"; done
```

- `gas/002-Extras.gs` は `python3 tools/build_extras.py` で組み立てる。
- `gas/004-WebApp.gs` は `python3 tools/build_webapp.py` で組み立てる。
- どちらも `gas/parts/` の中を直してから組み立て直すこと。
  組み立て後のファイルを直しても、次の組み立てで消える。
